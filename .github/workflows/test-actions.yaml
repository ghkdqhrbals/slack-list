name: Test Action

on:
  workflow_dispatch: # 수동 실행 트리거
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run my action
        uses: ./
        with:
          type: list
          messages: '["test1", "test2", "test3"]'
          channel-id: 'C088NNVGKGR'
          SLACK_WEBHOOK_URL: 'https://hooks.slack.com/services/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'